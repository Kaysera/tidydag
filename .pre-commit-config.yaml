repos:
  # Run your Taskfile lint check
  - repo: local
    hooks:
      - id: task-lint-check
        name: Task lint:check
        entry: bash -lc 'task lint:check'
        language: system
        pass_filenames: false
